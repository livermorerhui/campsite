<%= simple_form_for [@mission,@job] do |f| %>

<%= f.input :title %>
<%= f.input :description %>
<%= f.input :wage_upper_bound %>
<%= f.input :wage_lower_bound %>
<%= f.input :people_needed %>

  <%= f.submit "Submit" %>
<% end %>
