<%= simple_form_for [:admin, @mission] do |f| %>

<%= f.input :title %>
<%= f.input :description %>
<%= f.input :location %>

<%= f.submit "Submit" %>
<%= link_to 'Back', admin_missions_path %>

<% end %>
